class Solution {
    public int findFinalValue(int[] nums, int original) {
            int k = original;
            Set temp = new HashSet<>();
        for(int i : nums){
            temp.add(i);
    }

            while(temp.contains(k)){
                k*=2;
            }
            

    return k;
    }
}
